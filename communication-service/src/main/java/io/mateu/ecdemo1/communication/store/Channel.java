package io.mateu.ecdemo1.communication.store;

/**
 * How a recipient is told: in the inbox of its people, in their browsers (Web Push), by e-mail, or in
 * the Google Chat spaces it names.
 */
public enum Channel {
    INBOX, WEB_PUSH, EMAIL, GOOGLE_CHAT
}
